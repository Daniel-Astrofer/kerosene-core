[pytest]
testpaths = python
pythonpath =
    ../adapters/bitcoin_core_flask
    ../adapters/lightning_flask
addopts =
    -ra
markers =
    integration: docker-compose integration tests, skipped unless RUN_DOCKER_INTEGRATION=1
