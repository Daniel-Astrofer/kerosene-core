spring.application.name=v0.5
#Dados do banco
spring.datasource.url=jdbc:postgresql://localhost:5432/kerosene
spring.datasource.username=api_system
spring.datasource.password=blue-sky-black-ocean0
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Redis Server
spring.data.redis.host=127.0.0.1
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=2000

api.secret.aes.secret=${AES_SECRET}
api.secret.token.secret=${JWT_SECRET}

server.address=0.0.0.0



# Blockchain.info API configuration
blockchain.info.api-key=

# Bitcoin Deposit Configuration
# Seu endereço Bitcoin onde os usuários enviarão depósitos
bitcoin.deposit-address=1A1z7agoat7F9gq5TFGakzrx6R5vbR2rAP
# Número mínimo de confirmações para considerar depósito válido
bitcoin.min-confirmations=1
# Tempo de expiração do payment link em minutos
bitcoin.payment-link-expiration-minutes=60

# MODO MOCK - Ativa validações fake (para testes sem BTC real)
# Defina como true para testar sem usar Bitcoin real
bitcoin.mock-mode=true

# Logging levels for WebSocket diagnostics
logging.level.org.springframework.web=TRACE
logging.level.org.springframework.messaging=TRACE
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.socket=TRACE





