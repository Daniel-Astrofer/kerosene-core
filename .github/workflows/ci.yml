name: Core CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  compatibility:
    uses: Daniel-Astrofer/.github/.github/workflows/compatibility-ci.yml@2552ff2125c2d72accebe93804b8e139a52ded13
    with:
      component: core

  java:
    uses: Daniel-Astrofer/.github/.github/workflows/java-ci.yml@2552ff2125c2d72accebe93804b8e139a52ded13
    with:
      gradle-arguments: test dependencyCheckAnalyze

  jctl:
    uses: Daniel-Astrofer/.github/.github/workflows/admin-cli-ci.yml@c20b6773eaf68590ac128403c6959e0c758af1e4
    with:
      language: java
      package: kerosene-jctl

  security:
    uses: Daniel-Astrofer/.github/.github/workflows/security-scan.yml@2552ff2125c2d72accebe93804b8e139a52ded13

  # Adapter tests: enable when adapters have CI-ready test suites with all dependencies.
  # adapter-tests:
  #   name: adapter-tests
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       adapter: [bitcoin_core_backend, lightning_flask]
  #   defaults:
  #     run:
  #       working-directory: adapters/${{ matrix.adapter }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #     - name: Install dependencies
  #       run: pip install -r requirements.txt
  #     - name: Run tests
  #       run: python -m pytest tests/ -v
